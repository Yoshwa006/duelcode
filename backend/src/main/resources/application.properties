spring.application.name=comp

# Database Configuration - Use environment variables in production
spring.datasource.username=${DB_USERNAME:root}
spring.datasource.password=${DB_PASSWORD:test}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.url=jdbc:mysql://localhost:3306/comp
spring.jpa.hibernate.ddl-auto=update

# Show SQL statements
spring.jpa.show-sql=true

# Format SQL nicely
spring.jpa.properties.hibernate.format_sql=true

logging.level.org.apache.coyote.http11.Http11Processor=DEBUG

# JWT Configuration - Use environment variables in production
jwt.secret=${JWT_SECRET:1c5b7c2b8f8f4d48a1b7e5c2a6f8d1e0c8c12e3f0b6a9d4f5b2c8d3e4f6a1b2}
jwt.expiration=${JWT_EXPIRATION:86400000}

#redis
spring.data.redis.host=localhost
spring.data.redis.port=6379
spring.cache.type=redis

#elastic_search
spring.data.elasticsearch.repositories.enabled=true
spring.elasticsearch.uris=http://localhost:9200
spring.data.elasticsearch.connection-timeout=1s
spring.data.elasticsearch.read-timeout=30s

#mail - Use environment variables in production
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USERNAME:yoshwachess@gmail.com}
spring.mail.password=${MAIL_PASSWORD:dnzyvhjsqjjeagbx}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# Application Configuration
judge.api.url=${JUDGE_API_URL:http://localhost:3001}
app.mail.default-recipient=${DEFAULT_MAIL_RECIPIENT:}



